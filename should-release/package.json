{
  "name": "@microsoft/should-release",
  "version": "0.0.0",
  "private": true,
  "description": "Determines whether this release workflow run is needed, and cancels it if not",
  "scripts": {
    "build": "rimraf lib && tsc",
    "bundle": "rimraf dist && ncc build --source-map --license licenses.txt src/main.ts",
    "test": "jest"
  },
  "devDependencies": {
    "@actions/core": "^2.0.0",
    "@actions/glob": "^0.5.0",
    "@microsoft/beachball-actions-common": "0.3.0"
  }
}
